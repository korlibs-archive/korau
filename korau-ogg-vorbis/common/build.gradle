apply plugin: 'kotlin-platform-common'

dependencies {
    compile project(":korau-common")

    testCompile project(":korau-common")

    compile "com.soywiz:korio-common:$korioVersion"
    compile "com.soywiz:korma-common:$kormaVersion"
}
